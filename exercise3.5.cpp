#include <iostream>
#include <string>

using std::cin;
using std::cout;
using std::endl;
using std::string;

int main(){
	string line;
	while(cin>>line) //每次读入一个danci
	while(getline(cin, line)) //每次读入一行
//cout<<line<<endl;
	return 0;
}